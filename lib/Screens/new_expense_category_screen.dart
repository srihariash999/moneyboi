import 'package:flutter/material.dart';

class NewExpenseCategoryScreen extends StatelessWidget {
  const NewExpenseCategoryScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}